interface:
  display_name: "NIST Chatbot"
  short_description: "Repo-specific patterns and workflows for NIST_chatbot"
  default_prompt: "Use the NIST_chatbot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true